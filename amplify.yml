version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install aws-amplify@6.0.27 @aws-amplify/ui-react @aws-amplify/ui-components --force
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
